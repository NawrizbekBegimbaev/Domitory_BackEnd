name: dormitory_admin
description: "Dormitory Management System — Mobile Admin"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  shared_preferences: ^2.5.0
  provider: ^6.1.0
  cached_network_image: ^3.4.1
  flutter_secure_storage: ^9.2.0
  intl: ^0.20.0
  image_picker: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
